%script{ type: 'text/template', id: 'dataview-template' }
  %h2.title
    <%= title %>
  .viewport

%script{ type: 'text/template', id: 'time-control-template' }
  :plain
    <% _.each(timeRanges, function(range) {  %>
      <a href='#' data-title='<%= range.title %>'>
        <%= range.title %>
      </a>
    <% }) %>
